<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../../partials/head', { title: 'Gerenciar Usuários - E-DUCA' }) %>
</head>
<body>
    <div class="d-flex">
        <%- include('../partials/admin-sidebar', { currentPage: 'usuarios' }) %>
        
        <div class="main-content p-4">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="h3">Gerenciar Usuários</h1>
                <div class="text-muted">
                    Total: <strong><%= paginacao.totalUsuarios %></strong> usuários
                </div>
            </div>

            <div class="card shadow">
                <div class="card-header py-3">
                    <h6 class="m-0 fw-bold text-primary">Lista de Usuários</h6>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-bordered table-hover">
                            <thead class="table-light">
                                <tr>
                                    <th>ID</th>
                                    <th>Email</th>
                                    <th>Localização</th>
                                    <th>Data Cadastro</th>
                                    <th>Nível Acesso</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% usuarios.forEach(usuario => { %>
                                <tr>
                                    <td><%= usuario.id %></td>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <i class="bi bi-person-circle me-2 text-muted"></i>
                                            <%= usuario.email %>
                                        </div>
                                    </td>
                                    <td>
                                        <small>
                                            <%= usuario.cidade || 'N/A' %>
                                            <% if (usuario.estado) { %> - <%= usuario.estado %><% } %>
                                        </small>
                                    </td>
                                    <td>
                                        <small><%= usuario.data_cadastro.toLocaleDateString('pt-BR') %></small>
                                    </td>
                                    <td>
                                        <span class="badge 
                                            <%= usuario.nivel_acesso === 'superadmin' ? 'bg-danger' : 
                                                usuario.nivel_acesso === 'moderador' ? 'bg-warning' : 
                                                usuario.nivel_acesso === 'editor' ? 'bg-info' : 'bg-secondary' %>">
                                            <%= usuario.nivel_acesso %>
                                        </span>
                                        <% if (!usuario.is_admin && usuario.nivel_acesso === 'usuario') { %>
                                            <br><small class="text-muted">Usuário comum</small>
                                        <% } %>
                                    </td>
                                </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>

                    <% if (paginacao.totalPages > 1) { %>
                    <nav aria-label="Page navigation">
                        <ul class="pagination justify-content-center mt-4">
                            <% for (let i = 1; i <= paginacao.totalPages; i++) { %>
                            <li class="page-item <%= i === paginacao.paginaAtual ? 'active' : '' %>">
                                <a class="page-link" href="/admin/usuarios?page=<%= i %>"><%= i %></a>
                            </li>
                            <% } %>
                        </ul>
                    </nav>
                    <% } %>
                </div>
            </div>

            <div class="alert alert-info mt-4">
                <h6 class="alert-heading">
                    <i class="bi bi-info-circle me-2"></i>
                    Gerenciamento de Permissões
                </h6>
                <p class="mb-0">
                    Para alterar níveis de acesso dos usuários, utilize a página 
                    <a href="/admin/permissoes" class="alert-link">Gerenciar Permissões</a> 
                    disponível no menu administrativo avançado.
                </p>
            </div>
        </div>
    </div>

    <%- include('../../partials/footer') %>
</body>
</html>